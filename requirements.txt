# ── UI ────────────────────────────────────────────────────────────────────────
streamlit>=1.30

# ── Data handling ─────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
scipy>=1.10
openpyxl>=3.1

# ── ML / feature selection ────────────────────────────────────────────────────
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44

# ── Plotting ──────────────────────────────────────────────────────────────────
plotly>=5.0
matplotlib>=3.7

# ── Bayesian Optimisation (CPU-only torch to stay within cloud memory limits) ─
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
gpytorch==1.11
botorch==0.9.5
nextorch==0.4.0

# ── Report generation ─────────────────────────────────────────────────────────
# weasyprint>=60.0  # requires system libs (cairo, pango) — not available on cloud
